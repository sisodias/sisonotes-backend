# siso-notes-backend

**In one line:** Backend runtime for SISO Notes: CRDT doc persistence, quotas, auth, file ingestion, NAPI native modules. District: `SISO_Agency` (`~/SISO_Workspace/SISO_Agency/apps/SISOCRM/modules/siso-notes-backend`). Owner: not yet assigned; the top Agent Zero (herdr A0, SISO_Agents/agent-zero/siso-firstmate) holds it until it names one (siso-estate plan/owners.json).

The rest is in `README.md` if it has one. Find anything else on this machine with `estate where <words>`.
